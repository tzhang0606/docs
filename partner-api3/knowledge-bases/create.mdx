---
title: 'Create Knowledge Base'
api: 'POST /v1/knowledge-bases'
description: 'Create a new knowledge base for voice agents'
---

## Overview

Knowledge bases enhance voice agents with additional context from URLs, documents, and custom text. Agents automatically retrieve relevant information during calls without any prompt changes needed.

**Use cases**:
- Product FAQs
- Company policies
- Support documentation
- Pricing information
- Technical specifications

---

## Authentication

<ParamField header="X-API-Key" type="string" required>
  Your Teli API key
</ParamField>

---

## Request Body

<ParamField body="name" type="string" required>
  Knowledge base name (max 40 characters)
</ParamField>

<ParamField body="organization_id" type="string" required>
  Organization unique_id
</ParamField>

<ParamField body="user_id" type="string" required>
  User unique_id (creator)
</ParamField>

<ParamField body="urls" type="array">
  List of URLs to scrape and index. Max 500 URLs per knowledge base.
</ParamField>

<ParamField body="texts" type="array">
  List of text snippets. Each has `title` and `text`. Max 50 snippets per knowledge base.
</ParamField>

<ParamField body="auto_refresh" type="boolean">
  Enable auto-refresh for URLs (updates content every 24 hours). Default: false
</ParamField>

---

## Request Examples

<CodeGroup>

```bash cURL
curl -X POST "https://api.teli.ai/v1/knowledge-bases" \
  -H "X-API-Key: YOUR_API_KEY" \
  -H "Content-Type: application/json" \
  -d '{
    "name": "Product FAQ",
    "organization_id": "1762896364768x389173798861431550",
    "user_id": "1762896366429x599348279945132448",
    "urls": [
      "https://yourcompany.com/faq",
      "https://yourcompany.com/pricing"
    ],
    "auto_refresh": true
  }'
```

```javascript JavaScript
const response = await fetch('https://api.teli.ai/v1/knowledge-bases', {
  method: 'POST',
  headers: {
    'X-API-Key': 'YOUR_API_KEY',
    'Content-Type': 'application/json'
  },
  body: JSON.stringify({
    name: 'Product FAQ',
    organization_id: 'org_id_here',
    user_id: 'user_id_here',
    urls: [
      'https://yourcompany.com/faq',
      'https://yourcompany.com/pricing'
    ],
    auto_refresh: true
  })
})

const data = await response.json()
console.log(data)
```

```python Python
import requests

response = requests.post(
    'https://api.teli.ai/v1/knowledge-bases',
    headers={'X-API-Key': 'YOUR_API_KEY'},
    json={
        'name': 'Product FAQ',
        'organization_id': 'org_id_here',
        'user_id': 'user_id_here',
        'urls': [
            'https://yourcompany.com/faq',
            'https://yourcompany.com/pricing'
        ],
        'auto_refresh': True
    }
)

print(response.json())
```

</CodeGroup>

---

### With Text Snippets

<CodeGroup>

```json Text Snippets Example
{
  "name": "Company Policies",
  "organization_id": "org_id",
  "user_id": "user_id",
  "texts": [
    {
      "title": "Pricing Policy",
      "text": "Our standard pricing is $99/month. Enterprise pricing available for teams over 50 users. Annual plans get 20% discount."
    },
    {
      "title": "Support Hours",
      "text": "Support available Monday-Friday 9 AM - 6 PM EST. Emergency support available 24/7 for enterprise customers."
    }
  ]
}
```

</CodeGroup>

---

### URLs + Text Combined

<CodeGroup>

```json Combined Sources
{
  "name": "Complete Knowledge Base",
  "organization_id": "org_id",
  "user_id": "user_id",
  "urls": [
    "https://yourcompany.com/docs"
  ],
  "texts": [
    {
      "title": "Quick Facts",
      "text": "Founded in 2020. 500+ customers. 99.9% uptime SLA."
    }
  ],
  "auto_refresh": true
}
```

</CodeGroup>

---

## Response

<ResponseField name="success" type="boolean">
  Whether creation was successful
</ResponseField>

<ResponseField name="knowledge_base_id" type="string">
  Unique ID of the created knowledge base. Use this to link to agents.
</ResponseField>

<ResponseField name="unique_id" type="string">
  Your platform's unique ID (for internal reference)
</ResponseField>

<ResponseField name="name" type="string">
  Knowledge base name
</ResponseField>

<ResponseField name="status" type="string">
  Processing status: `in_progress`, `complete`, or `error`
</ResponseField>

<ResponseField name="total_sources" type="number">
  Number of sources added
</ResponseField>

<ResponseField name="message" type="string">
  Success message
</ResponseField>

<ResponseField name="powered_by" type="string">
  Platform identifier ("Teli")
</ResponseField>

<ResponseExample>

```json 201 - Created
{
  "success": true,
  "knowledge_base_id": "knowledge_base_7a0682ce5fc02413",
  "unique_id": "1764105884537x291772143525964264",
  "name": "Product FAQ",
  "status": "in_progress",
  "total_sources": 2,
  "message": "Knowledge base created successfully",
  "powered_by": "Teli"
}
```

```json 400 - Validation Error
{
  "error": "name is required",
  "success": false,
  "powered_by": "Teli"
}
```

```json 404 - Organization Not Found
{
  "error": "Organization not found",
  "success": false,
  "powered_by": "Teli"
}
```

```json 500 - Creation Failed
{
  "error": "Failed to create knowledge base",
  "success": false,
  "powered_by": "Teli"
}
```

</ResponseExample>

---

## Processing Status

After creation, the knowledge base status will be `in_progress`.

**Processing includes**:
- Scraping URLs
- Chunking content
- Creating embeddings
- Indexing for retrieval

**Timeline**: Usually 1-5 minutes depending on content size

**Status changes**:
```
⏳ in_progress → ✅ complete (ready to use)
⏳ in_progress → ❌ error (processing failed)
```

**Check status**: Use [Get Knowledge Base](/api-reference/knowledge-bases/get) endpoint

---

## Limits

<Warning>
**Per knowledge base limits:**
- URLs: Max 500
- Text snippets: Max 50
- Files: Max 25 (50MB each) - Use dashboard for file upload
- Name: Max 40 characters

**Create multiple knowledge bases** to overcome these limits. Agents can link to multiple KBs.
</Warning>

---

## Auto-Refresh

When `auto_refresh` is enabled:
- URLs are re-fetched every 24 hours
- Content automatically stays up-to-date
- Useful for frequently changing documentation

<Info>
Auto-refresh only applies to URL sources, not text snippets.
</Info>

---

## Next Steps

**After creating a knowledge base**:
1. Wait for status to become `complete` (check via GET endpoint)
2. Link to voice agent via `knowledge_base_ids` parameter
3. Test calls to verify agent uses KB context

**See**: [Link Knowledge Base to Agent](/api-reference/agents/create#knowledge-base)

---

## Related Endpoints

- [List Knowledge Bases](/api-reference/knowledge-bases/list) - View all KBs
- [Get Knowledge Base](/api-reference/knowledge-bases/get) - Get details
- [Delete Knowledge Base](/api-reference/knowledge-bases/delete) - Remove KB
- [Create Voice Agent](/api-reference/agents/create) - Link KB to agent
- [Update Voice Agent](/api-reference/agents/update) - Update agent's KBs

---

**Enhance your voice agents with knowledge bases!** 🚀

